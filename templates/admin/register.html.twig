{% extends 'base.html.twig' %}

{% block title %}Admin - Register{% endblock %}

{% block body %}
    <div class="container">
        {# tuto YTM #}
        {{ include('menu/admin_menu2.html.twig') }}
        <h1>Admin - Inscription d'un nouvel employé</h1>

        {{ form_errors(registrationForm) }}

        {{ form_start(registrationForm) }} 
        {# <-> rappel: registrationForm de RegistrationController #}
            {{ form_row(registrationForm.mail) }}
            {{ form_row(registrationForm.password, {label: 'Password'}) }}
            {# {{ form_row(registrationForm.plainPassword) }} #}
            {{ form_row(registrationForm.nom) }}
            {{ form_row(registrationForm.prenom) }}
            {# {{ form_row(registrationForm.agreeTerms) }} #}

            <button type="submit" class="btn btn-primary">Inscription d'un nouvel employé</button>
            
        {{ form_end(registrationForm) }}

    </div>
{% endblock %}


