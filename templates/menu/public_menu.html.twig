{# boostrap navbar<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <nav class="navbar navbar-dark bg-dark">
            <!-- Navbar content -->
            <ul>
                <li> <a class="navbar-brand">Navbar</a> </li> 
                <form class="form-inline">
                <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
                </form> 
                <li><a class="contact">Contact</a></li>
                <li><a class="contact">Espace pro</a></li>
            </ul>
        </nav>#}

<div class="row">
    <nav class="navbar navbar-dark main_nav">

        <span class="float-left">
            {# <a href="#" class="btn btn-nav">Contact</a> #}
            {# TOODO #}
        </span>

        <span class="float-right">
            {# tuto YTM #}
            {# /si on est connecté, il ya des élements dans app.user #}
            {% if app.user %} 
                <a href="{{ path("admin_index") }}" class="btn btn-nav_public_part">Admin</a>
                <a href="{{ path("app_logout") }}" class="btn btn-nav_public_part" id="btn-deco">Déconnexion</a>
            {% else %}
                <a href="{{ path("app_login") }}" class="btn btn-nav_public">Espace pro</a>
            {% endif %}
        </span>

    </nav>
</div>

